# Do not auto-install peer deps (npm 7+ default). rxjs and @reduxjs/toolkit are
# peers of observable-hooks / verdux; we deliberately let them resolve from the
# repo-root node_modules so there is a single shared copy. A duplicate rxjs in
# this folder's node_modules causes nominal type clashes against verdux's src.
legacy-peer-deps=true
